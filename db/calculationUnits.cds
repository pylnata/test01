
using {
    managed,
    Currency,
    temporal,
    Country,
    extensible,
    cuid,
    sap.common.CodeList
} from '@sap/cds/common';

using { function } from './commonAspects';

entity CalculationUnits : managed, function {
}

