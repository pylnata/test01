
using {
    managed,
    Currency,
    temporal,
    Country,
    extensible,
    cuid,
    sap.common.CodeList
} from '@sap/cds/common';

using { keyFunction } from './commonAspects';

entity CalculationUnits : managed, keyFunction {
}

